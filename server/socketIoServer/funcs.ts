import prisma from "@/lib/prisma";

const funcs = {
  appendMessage: async (message: string, userId: number) => {
    try {
    const appendedMessage = await prisma.conversation 
    } catch (error) {
        
    }
  },
};
